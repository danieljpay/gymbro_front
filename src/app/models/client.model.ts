import { DeclarationListEmitMode } from "@angular/compiler";

export interface Client {
    id: number;
    address: string;
    email: string;
    last_name: string;
    name: string;
    phone: number;
}